<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="tax_isr" model="account.tax">
        <field name="name">ISR Retencion</field>
        <field name="description">RET(5%, 7%)</field>
        <field name="amount">0.05</field>
        <field name="amount_type">code</field>
        <field name="type_tax_use">purchase</field>
        <field name="tax_exigibility">on_payment</field>
        <field name="python_compute">if price_unit > 30000:
                                        result = 30000 * -0.05 * quantity
                                        result += (price_unit/1.12 - 30000) * -0.07 * quantity
                                     else: 
                                        result = price_unit/1.12 * -0.05 * quantity
        </field>
    </record>
</odoo>
